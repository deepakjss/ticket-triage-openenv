spec_version: 1
name: ticket_triage_openenv
type: space
runtime: fastapi
app: server.app:app
# Default app port inside the image (OpenEnv docker maps host:8000). HF sets PORT=7860 at runtime.
port: 8000
